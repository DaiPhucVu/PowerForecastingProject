# Pinned for reproducibility. Resolved together on Python 3.13 (Windows x64).
# Change a pin only in its own pull request, and re-run the pipeline after.
numpy==2.1.3
pandas==2.3.2
scikit-learn==1.7.2
xgboost==3.0.5
tensorflow==2.20.0
keras==3.15.1
shap==0.48.0
matplotlib==3.10.6
